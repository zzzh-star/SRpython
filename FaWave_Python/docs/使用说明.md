# FaWave 使用方法

## 基本配置
1. 左侧面板设置设备 IP 和端口号 (默认: `192.168.1.82:16008`)
2. 模式切换：可以通过顶部切换 "牵拉模式" 或 "剪切模式"
3. 点击 "建立连接" 开始采集。

## 3D 视图与图表交互
* 3D 视图支持鼠标拖拽旋转和滚轮缩放。
* 数据总览面板显示四路原始电压与三维力解耦数值。

## 记录与保存
* 勾选 "开启本地存储"，系统将以异步方式保存数据（CSV 或 XLSX），不影响界面流畅度。
